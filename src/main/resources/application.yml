spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  datasource:
    # 데이터 소스 설정: 메모리 내 H2 데이터베이스 사용
    url: jdbc:h2:mem:testdb
  h2:
    console:
      enabled: true